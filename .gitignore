# Private working notes — local only, never pushed
HANDOFF.md
ROADMAP.md

# Node deps for tools/ — never committed, never deployed
node_modules/

# Local preview config for Claude Code, not part of the site
.claude/
